# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
websockets==13.0.1

# Settings & validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Database
sqlalchemy==2.0.35
alembic==1.13.2
# psycopg2-binary==2.9.9  # uncomment when DATABASE_URL points at Postgres

# Data / analysis
pandas==2.2.3
numpy==1.26.4

# HTTP client (Anthropic API, news calendar, Telegram)
httpx==0.27.2

# Telegram bot
python-telegram-bot==21.6

# Scheduler for background jobs (price polling, news checks, daily summary)
apscheduler==3.10.4

# Security
cryptography==43.0.1

# Charts (server-side snapshot rendering for Telegram alerts / journal exports)
plotly==5.24.1
kaleido==0.2.1

# Env loading
python-dotenv==1.0.1

# Logging
python-json-logger==2.0.7

# Dev / test / lint
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx[cli]==0.27.2
ruff==0.6.9
black==24.8.0
mypy==1.11.2

# MetaTrader5 SDK — Windows-only wheel, only installable/importable on a
# Windows host running a real MT5 terminal. Left commented so `pip install
# -r requirements.txt` succeeds on Linux/macOS dev & CI machines; the code
# in services/broker/mt5_gateway.py imports it lazily and raises a clear
# BrokerUnavailableError if it's missing. Uncomment on the Windows box that
# actually runs the live broker connection.
# MetaTrader5==5.0.4874
